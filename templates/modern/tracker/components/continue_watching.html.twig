<div class="card mb-4 shadow-sm border-0">
    <div class="card-header fw-semibold d-flex justify-content-between align-items-center">
        <span>Continue Watching</span>

        <div class="cw-arrows d-flex align-items-center gap-3">
            <span id="cw-prev" class="cw-arrow">&lsaquo;</span>
            <span id="cw-next" class="cw-arrow">&rsaquo;</span>
        </div>
    </div>

    <div class="card-body">

        <style>
            .fade-soft {
                opacity: 0;
                transition: opacity 0.35s ease-in-out;
            }
            .fade-soft.show {
                opacity: 1;
            }
            .badge-overlay {
                position: absolute;
                top: 8px;
                right: 8px;
                z-index: 2;
            }
            .cw-hidden {
                display: none !important;
            }
            .cw-show-more {
                height: 100%;
                border: 2px dashed #ccc;
                border-radius: .5rem;
                display: flex;
                align-items: center;
                justify-content: center;
                font-weight: 600;
                color: #666;
                cursor: pointer;
                transition: background 0.2s;
            }
            .cw-show-more:hover {
                background: #f5f5f5;
            }
        </style>
        <style>
            .cw-arrow {
                font-size: 1.6rem;
                line-height: 1;
                cursor: pointer;
                user-select: none;
                color: #666;
                transition: color 0.2s, transform 0.2s;
            }
            .cw-arrow:hover {
                color: #000;
                transform: scale(1.15);
            }
        </style>
        <style>
            #cw-grid {
                transition: transform 0.45s ease;
                will-change: transform;
            }
        </style>
        <style>
            .progress-bar {
                transition: width 0.6s ease;
            }
        </style>




        {% if continueWatching is empty %}
            <p class="text-muted small">No ongoing shows.</p>
        {% else %}

            <div id="cw-grid" class="d-flex flex-row gap-3 fade-soft show" style="overflow-x: hidden;">

                {% for item in continueWatching %}
                    <div class="cw-item" style="flex: 0 0 calc(100% / 7);">
                        <div class="card h-100 border-0 shadow-sm position-relative"
                             data-id="{{ item.tvId }}">

                            <div class="badge-overlay">
                                {% if item.status == 'completed' %}
                                    <span class="badge bg-success">Completed</span>
                                {% elseif item.status == 'in_progress' %}
                                    <span class="badge bg-primary">Continue</span>
                                {% else %}
                                    <span class="badge bg-warning text-dark">New</span>
                                {% endif %}
                            </div>

                            {% if item.coverUrl %}
                                <img src="{{ item.coverUrl }}" loading="lazy" class="card-img-top" alt="{{ item.title }}">
                            {% else %}
                                <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center"
                                     style="height: 180px; border-radius: .25rem;">
                                    <span class="text-white-50 small">No Image</span>
                                </div>
                            {% endif %}

                            <div class="card-body">
                                <h5 class="card-title">{{ item.title }}</h5>

                                {% if item.nextEpisode %}
                                    <p class="small text-muted mb-2">
                                        Next: S{{ item.nextEpisode.season }}E{{ item.nextEpisode.number }}
                                    </p>
                                {% else %}
                                    <p class="small text-muted mb-2">Up to date</p>
                                {% endif %}

                                <div class="progress mb-2" style="height: 4px;">
                                    <div class="progress-bar bg-primary"
                                         role="progressbar"
                                         style="width: {{ item.progressPercent }}%;"
                                         aria-valuenow="{{ item.progressPercent }}"
                                         aria-valuemin="0"
                                         aria-valuemax="100">
                                    </div>
                                </div>

                                <div class="d-flex gap-2 mt-2">
                                    {% if item.nextEpisode %}
                                        <button class="btn btn-sm btn-primary cw-mark-watched"
                                                data-id="{{ item.tvId }}"
                                                data-season="{{ item.nextEpisode.season }}"
                                                data-episode="{{ item.nextEpisode.number }}">
                                            Mark Episode Watched
                                        </button>

                                        <button class="btn btn-sm btn-outline-primary cw-mark-season"
                                                data-id="{{ item.tvId }}"
                                                data-season="{{ item.nextEpisode.season }}">
                                            Mark Season {{ item.nextEpisode.season }} Watched
                                        </button>
                                    {% endif %}

                                    <button class="btn btn-sm btn-outline-secondary cw-undo-watched"
                                            data-id="{{ item.tvId }}">
                                        Undo Watched
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}

                <div class="cw-show-more-tile cw-hidden" style="flex: 0 0 calc(100% / 7);">
                    <div class="cw-show-more">
                        Show More &rsaquo;
                    </div>
                </div>

            </div>

        {% endif %}
    </div>
</div>
