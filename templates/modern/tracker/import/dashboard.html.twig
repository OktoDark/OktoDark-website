{% extends '@theme/base.html.twig' %}

{% block title %}Import Dashboard{% endblock %}
{% block head %}{{ parent() }}{% endblock %}
{% block body_id %}tracker{% endblock %}

{% block main %}

    <!-- PAGE TITLE -->
    <section class="section section-page-title bg-default text-center py-4">
        <div class="container">
            <h2 class="display-6 fw-bold">Import Dashboard</h2>
            <p class="lead text-muted mt-2">Choose an import source to get started</p>
        </div>
    </section>

    <section class="section section-lg">
        <div class="container-fluid px-4">
            <div class="row justify-content-center">

                <!-- SIDEBAR -->
                {% include '@theme/tracker/partials/_sidebar.html.twig' %}

                <!-- MAIN CONTENT -->
                <div class="col-md-9">

                    <!-- IMPORT CARDS -->
                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title fw-bold">Import TV</h5>
                                    <p class="card-text text-muted small">
                                        Upload Trakt JSON or CSV exports to import TV shows, seasons, and episodes.
                                    </p>
                                    <a href="{{ path('app_import_tv') }}"
                                       class="btn btn-primary text-bg-primary mt-auto align-self-start">
                                        Go to TV Import
                                    </a>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title fw-bold">Import Movies</h5>
                                    <p class="card-text text-muted small">
                                        Upload Trakt, TVTime, Letterboxd, Simkl, or custom CSV/JSON files to import movies.
                                    </p>
                                    <a href="{{ path('app_import_movies') }}"
                                       class="btn btn-primary text-bg-primary mt-auto align-self-start">
                                        Go to Movie Import
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </section>

    {% block javascripts %}
        {{ parent() }}
    {% endblock %}

{% endblock %}
