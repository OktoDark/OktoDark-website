{% block trix_editor_widget %}
    <input id="{{ id }}" type="hidden" name="{{ full_name }}" value="{{ value }}">
    <trix-editor input="{{ id }}" class="trix-editor-modern"></trix-editor>
{% endblock %}
