{% extends "@theme/base.html.twig" %}

{% block title %}{{ 'title.member'|trans }}{% endblock %}

{% block head %}
    {{  parent() }}
{% endblock %}

{% block body %}

    <section class="section section-page-title context-dark">
        <div class="container">
            <h2>{{ 'title.member'|trans }}</h2>
        </div>
    </section>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {{ render(controller('App\\Controller\\MemberController:viewPage')) }}
    {% else %}
        {% include('@theme/form/login.html.twig') %}
    {% endif %}

{% endblock %}