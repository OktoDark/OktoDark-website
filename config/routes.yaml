##################################
#
# Main Routes
#
##################################
homepage:
    path: /{_locale}
    controller: App\Controller\HomeController:home
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

home:
    path: /{_locale}/home
    controller: App\Controller\HomeController:home
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

games:
    path: /{_locale}/games
    controller: App\Controller\GamesController::games
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

careers:
    path: /{_locale}/careers
    controller: App\Controller\CareersController::careers
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

blog:
    path: /{_locale}/blog
    controller: App\Controller\BlogController::index
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

##################################
#
# Redirects Routes
#
##################################
forum:
    path: /{_locale}/forum
    controller: App\Controller\RedirectController::forum
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

##################################
#
# Support Routes
#
##################################
bedonator:
    path: /{_locale}/bedonator
    controller: App\Controller\SupportusController::bedonator
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

bepatron:
    path: /{_locale}/bepatron
    controller: App\Controller\SupportusController::bepatron
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

##################################
#
# Members Area Routes
#
##################################
member:
    path: /{_locale}/member
    controller: App\Controller\MemberController::member
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

settings:
    path: /{_locale}/settings
    controller: App\Controller\MemberController::settings
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

##################################
#
# Informations Routes
#
##################################
about:
    path: /{_locale}/about
    controller: App\Controller\InfoController::about
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

contact:
    path: /{_locale}/contact
    controller: App\Controller\InfoController::contact
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

faq:
    path: /{_locale}/faq
    controller: App\Controller\InfoController::faq
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

services:
    path: /{_locale}/services
    controller: App\Controller\InfoController::services
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

privacy-policy:
    path: /{_locale}/privacy-policy
    controller: App\Controller\InfoController::privacypolicy
    # schemes:    [https]
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'