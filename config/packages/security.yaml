security:
    # https://symfony.com/doc/current/security.html#c-hashing-passwords
    password_hashers:
        # By default, password hashers are resource intensive and take time. This is
        # important to generate secure password hashes. In tests however, secure hashes
        # are not important, waste resources and increase test times. The following
        # reduces the work factor to the lowest possible values.
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
            algorithm: argon2id
            cost: 4 # Lowest possible value for bcrypt
            time_cost: 3 # Lowest possible value for argon
            memory_cost: 10 # Lowest possible value for argon

    providers:
        # https://symfony.com/doc/current/security.html#b-configuring-how-users-are-loaded
        # In this example, users are stored via Doctrine in the database
        # To see the users at src/App/DataFixtures/ORM/LoadFixtures.php
        # To load users from somewhere else: https://symfony.com/doc/current/security/user_providers.html#using-a-custom-query-to-load-the-user
        database_users:
            entity: { class: App\Entity\User, property: email }

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        main:
            # this firewall does not have a 'pattern' option because it applies to all URLs
            # pattern: ^/[a-z]{2}/
            lazy: true
            provider: database_users
            custom_authenticator: App\Security\FormAuthenticator

            remember_me:
              secret: '%kernel.secret%'
              lifetime: 604800 # 1 week in seconds
              path: /
              remember_me_parameter: _remember_me
              always_remember_me: false
              token_provider: 'Symfony\Bridge\Doctrine\Security\RememberMe\DoctrineTokenProvider'

            logout:
                path: security_logout
                target: home_index

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: '^/[a-z]{2}/login', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/register', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/reset-password', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/verify/email', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/member/profile/[A-Za-z0-9._-]+$', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/live-users$', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/blog', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/forum', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/games', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/careers', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/supportus', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/contact', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/streaming', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/mods', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/info', roles: PUBLIC_ACCESS }
        - { path: '^/[a-z]{2}/member', roles: ROLE_USER }
        - { path: '^/[a-z]{2}/admin', roles: ROLE_USER }
        - { path: ^/debug/permissions, roles: ROLE_ADMIN }

    role_hierarchy:
        ROLE_SUPER_ADMIN: [ ROLE_ADMIN ]

when@test:
    # this configuration simplifies testing URLs protected by the security mechanism
    # See https://symfony.com/doc/current/testing/http_authentication.html
    security:
        firewalls:
            main:
                http_basic: ~
                entry_point: http_basic

