# config/packages/mailer.yaml
framework:
    mailer:
        transports:
            main: '%env(MAILER_DSN)%'
            no_reply: '%env(MAILER_DSN)%'
            contact: '%env(MAILER_DSN)%'
            jobs: '%env(MAILER_DSN)%'
