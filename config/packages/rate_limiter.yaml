framework:
    rate_limiter:
        resend_verification:
            policy: fixed_window
            limit: 5
            interval: '1 hour'

        contact_form:
            policy: fixed_window
            limit: 3
            interval: '1 hour'
