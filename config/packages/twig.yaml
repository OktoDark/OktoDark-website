twig:
    # globals:

    debug:            '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    # auto_reload:      ~

    # Templates
    paths:
        # uncomment if you use or comment if not use
        # default
        '%kernel.project_dir%/templates/default': 'theme'
        # grey
        # '%kernel.project_dir%/templates/grey': 'theme'